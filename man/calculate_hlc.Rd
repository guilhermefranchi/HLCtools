% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_hlc.R
\name{calculate_hlc}
\alias{calculate_hlc}
\title{Calculate interval-level Herd Lying Concordance}
\usage{
calculate_hlc(
  data,
  group,
  animal,
  interval,
  lying,
  day = NULL,
  period = NULL,
  interval_min = 15,
  methods = c("sd", "mad", "iqr", "entropy"),
  lying_threshold = interval_min/2,
  sync_thresholds = c(0.6, 0.7, 0.8, 0.9),
  add_lying_weighted = TRUE
)
}
\arguments{
\item{data}{A data frame containing individual animal behaviour records.}

\item{group}{Column identifying the herd, treatment, pen, or group.}

\item{animal}{Column identifying the individual animal.}

\item{interval}{Column identifying the time interval within day.}

\item{lying}{Column containing lying time within the interval, in minutes.}

\item{day}{Optional column identifying day/date.}

\item{period}{Optional column identifying a larger period, for example week.}

\item{interval_min}{Length of the observation interval in minutes. Default is 15.}

\item{methods}{Character vector of HLC methods to compute. Options are
\code{"sd"}, \code{"mad"}, \code{"iqr"}, and \code{"entropy"}.}

\item{lying_threshold}{Threshold in minutes used to define binary lying state
for entropy. Default is half of \code{interval_min}.}

\item{sync_thresholds}{Numeric vector of threshold synchrony cut-offs, expressed
as proportions. Default is \code{c(0.6, 0.7, 0.8, 0.9)}.}

\item{add_lying_weighted}{Logical. If TRUE, adds lying-weighted HLC metrics.}
}
\value{
A tibble with one row per group-time interval.
}
\description{
Calculates Herd Lying Concordance (HLC) metrics from individual animal
lying-time records. HLC is calculated within each group-time interval as
one minus a normalised measure of between-animal dispersion.
}
\examples{
data(example_lies)

hlc_intervals <- calculate_hlc(
  data = example_lies,
  group = group,
  animal = cow,
  day = day,
  period = week,
  interval = time,
  lying = lying,
  interval_min = 15,
  methods = c("sd", "mad", "iqr", "entropy"),
  sync_thresholds = c(0.6, 0.7, 0.8, 0.9),
  add_lying_weighted = TRUE
)

hlc_intervals

}
